# Fix-batch — 2026-05-24 walkthrough punch-list

## Status: IN PROGRESS

Source of truth for what's getting fixed and in what wave. Ticks land
with the jj change id (or git short-sha) on the phase header.

---

## Wave 1 — shipped in change `<sha-tbd>`

- [x] **W1.1** Density-row added to Week view (3-day + Day already
      had it; Month skipped — different layout).
- [ ] **W1.2** **R-3** — Week + 3-day proper multi-column: the
      `ScheduleWeekView` / `ScheduleThreeDayView` grids exist already;
      the user's screenshot was the Stacked layout. Document the
      Layout-toggle FAB as the way to switch. Punted to wave 2 if the
      Stacked default isn't right.
- [x] **W1.3** **B-3** — `OverlayResolver` gates supersedence on
      `band.instance.source is InstanceSource.RuleInstance`. One-off
      events stay visible. Tests updated to use a daily rule for the
      supersedence assertion + a new explicit "one-off skipped" test.
- [x] **W1.4** **R-1** — Day-view band label now shows the band's RAW
      start/end times with `↳` prefix when it continues from before
      the window, `↴` suffix when it extends past. Position + height
      still clip to the visible window.
- [x] **W1.5** **R-2** — `EventDetailSheet` shows
      `Sat May 23 10:00 → Sun May 24 20:00` for multi-day events.
- [x] **W1.6** **U-5** — `cd_identity_avatar` string is now
      `Switch repository` (was `Identity`).
- [x] **W1.7** **U-6** — `settings_category_mode` renamed
      `Lifestyle` → `Mode` so it doesn't collide with the actual
      Lifestyle category label.
- [x] **W1.8** **R-6** — Settings → Calendars row footer drops the
      `repo: <uuid>` prefix; shows `priority N` only. Full repo-name
      threading deferred (needs reposFlow plumbing).

## Wave 2 — bigger surface, ship next

- [x] **W2.1** **B-4** — shipped in `6b6a08d`. Root cause was demo data:
      holidays' `supersedes` list was missing `base` (00b) and `collar`
      (00c). All four bands the user reported (`Grooming + breakfast`
      → base, `Commute in/home` → base+commute, `Collar on (home)` →
      collar) live on calendars not in the list. Wiring itself works
      correctly (verified via temporary `skb.overlay` Log.d in
      `OverlayResolver.suppressorIntervalsByRef` + `suppressedByMap`
      — both reflect the corrected demo data after the fix). Added
      `base` + `collar` to `calendars/holidays/calendar.toml`
      `supersedes`. Debug logs stripped before commit.
- [x] **W2.2** **B-2** — Wizard finish creates the repo: `RepoStore.add`
      + `setDefaultWriteRepoName` were already wired in
      `MainActivity.onWizardScaffold`; missing piece was the demo-off
      flip. Added `graph.demoModePrefs.setActive(false)` at the end of
      the scaffold chain so the new repo becomes the sole write target
      atomically and the schedule re-renders against it. Top-bar avatar
      already keys on the active repo's `iconSpecies`, so it follows
      `defaultWriteRepoName` automatically.
- [x] **W2.3** **R-5** — Added `todolistsFlow: StateFlow<List<TodolistMeta>>`
      to `AppGraph` (derived from `snapshot.map { it.todolists }`),
      plumbed through `SettingsAccess.todolistsFlow`, and added a new
      `TodolistsCategoryMaster` composable mirroring
      `CalendarsCategoryMaster`. Settings → Todolists now reads from
      the live indexer registry instead of the user-toggled subset.
- [x] **W2.4** **U-8/U-9/U-10** — Renamed wizard string
      `wizard_default_calendar_got_it` from "Got it" to "Continue".
      Step counter now filters `SCREEN_ORDER` to user-visible screens
      only (excludes `Scaffold` which auto-progresses; excludes
      `ShareWithDom` unless `shouldShowShareWithDom(draft)`), so the
      "Step N of M" label matches what the user actually sees.
- [x] **W2.5** **M-3** — Now tab pre-filters today's bands inside
      `SchedulePane`: drops bands whose `effectiveEnd <= now`, leads
      with the currently-in-progress band (start <= now < end), then
      upcoming. Other days render unchanged. AVD-verified — Brighton
      weekend 10:00–20:00 leads the agenda on Sun May 24 19:13.

## Wave 3 — polish + small follow-ups

- [ ] **W3.1** **R-4** — Cascade-step zoom-aware so dense morning
      stacks (8 bands in 90 min) don't overlap unreadably.
- [ ] **W3.2** **R-7** — Tasks "Overdue (21)" on fresh install:
      either bound the overdue window or rename the bucket.
- [ ] **W3.3** **R-8** — Reviews timestamps to human-friendly
      ("2 days ago" / "Sat 20:30").
- [ ] **W3.4** **U-1** — Intro Continue button click area extends
      to bottom screen edge.
- [ ] **W3.5** **U-2** — Superseded-mode FAB tooltip / better
      content-description on long-press.
- [ ] **W3.6** **U-3** — "Now/Day/3-day/Week/Month" rail labels
      stop overlapping band content.
- [ ] **W3.7** **U-4** — Intro step-2 "Other scenarios" rows either
      become tappable or visually de-emphasized.
- [ ] **W3.8** **U-7** — Settings "Accounts" tile renamed to "Repos"
      / "Repositories" to match the rest of the app vocabulary.
- [ ] **W3.9** **U-11** — Wizard sticker pack lists all installed
      species (currently Bat + Bunny only).
- [ ] **W3.10** **U-12** — Settings → Calendars hide the meta-level
      "Base layers" row OR rename the demo's `base` calendar.
- [ ] **W3.11** **M-1** — FAB long-press tooltips.
- [ ] **W3.12** **M-2** — `adjustToLocalTimezone` OFF banner that
      explains the warped wall-clock to non-UK users.
- [ ] **W3.13** **M-4** — Reviews "mark all read" action.
- [ ] **W3.14** **M-5** — Event detail zone display: friendly format
      instead of raw IANA `Arctic/Longyearbyen`.

## Out of scope (won't fix this round, won't promise)

- Tablet master-detail completeness — own round.
- Performance / cold-start.
- Real-device wifi-adb runs.
- CalDAV / GitHub OAuth driving.
