# Fix-batch — 2026-05-24 walkthrough punch-list

## Status: IN PROGRESS

Source of truth for what's getting fixed and in what wave. Ticks land
with the jj change id (or git short-sha) on the phase header.

---

## Wave 1 — shipped in change `<sha-tbd>`

- [x] **W1.1** Density-row added to Week view (3-day + Day already
      had it; Month skipped — different layout).
- [ ] **W1.2** **R-3** — Week + 3-day proper multi-column: the
      `ScheduleWeekView` / `ScheduleThreeDayView` grids exist already;
      the user's screenshot was the Stacked layout. Document the
      Layout-toggle FAB as the way to switch. Punted to wave 2 if the
      Stacked default isn't right.
- [x] **W1.3** **B-3** — `OverlayResolver` gates supersedence on
      `band.instance.source is InstanceSource.RuleInstance`. One-off
      events stay visible. Tests updated to use a daily rule for the
      supersedence assertion + a new explicit "one-off skipped" test.
- [x] **W1.4** **R-1** — Day-view band label now shows the band's RAW
      start/end times with `↳` prefix when it continues from before
      the window, `↴` suffix when it extends past. Position + height
      still clip to the visible window.
- [x] **W1.5** **R-2** — `EventDetailSheet` shows
      `Sat May 23 10:00 → Sun May 24 20:00` for multi-day events.
- [x] **W1.6** **U-5** — `cd_identity_avatar` string is now
      `Switch repository` (was `Identity`).
- [x] **W1.7** **U-6** — `settings_category_mode` renamed
      `Lifestyle` → `Mode` so it doesn't collide with the actual
      Lifestyle category label.
- [x] **W1.8** **R-6** — Settings → Calendars row footer drops the
      `repo: <uuid>` prefix; shows `priority N` only. Full repo-name
      threading deferred (needs reposFlow plumbing).

## Wave 2 — bigger surface, ship next

- [ ] **W2.1** **B-4** — Spring bank holiday + Christmas + similar
      all-day base events suppress dependent stack. **Investigated
      2026-05-24, partial finding:** after wave 1 the holiday rule
      DOES materialize and the band IS visible on Mon May 25
      ("00:00 Spring bank holiday (UK)" appears). But the
      supersedence pass doesn't fire — `Grooming + breakfast 06:30`,
      `Commute in 08:15`, `Commute home 17:00`, `Collar on (home)
      17:45` all still render. Confirmed holidays' `supersedes` list
      DOES reach `CalendarMeta` (`CalendarRegistry.kt:189`). Suspect
      the all-day-spans-24h interval `2026-05-25T01:00 device →
      2026-05-26T01:00 device` overlaps these bands fine, so the
      bug is elsewhere — possibly `kind=base` + `non_superseable=true`
      interacting weirdly, OR the holidays calendar isn't in
      `activeCalendars`, OR the materialized instance carries a
      different `calendar` ref than expected. Add a debug log to
      `OverlayResolver.suppressorIntervalsByRef` to confirm what's
      detected, then iterate.
- [ ] **W2.2** **B-2** — Wizard finish actually creates the repo:
      flip demo off, add new repo via `RepoStore.add`, set as
      `defaultWriteRepoName`, switch top-bar avatar.
- [ ] **W2.3** **R-5** — Settings → Todolists reads from live indexer
      registry instead of empty wizard-side prefs. Today reads from
      `CalendarVisibilityPrefs.state.ordered` which only holds entries
      the user has explicitly toggled. Needs a parallel `todolistsFlow`
      threaded through `SettingsAccess` the same way `calendarsFlow`
      already is for the Calendars category.
- [ ] **W2.4** **U-8/U-9/U-10** — Wizard step-counter accuracy + Step 9
      "Got it" button becomes "Continue".
- [ ] **W2.5** **M-3** — Now tab shows in-progress at top + upcoming
      below.

## Wave 3 — polish + small follow-ups — shipped in commit `32f78f9`

- [x] **W3.1** **R-4** — Cascade-step zoom-aware: ScheduleDayView
      now scales the per-lane cascade offset with `effectiveZoom`
      (28dp at zoom 1, 40dp at zoom 2, 56dp at zoom ≥ 3) so dense
      morning stacks read cleanly at low zoom.
- [x] **W3.2** **R-7** — Tasks "Overdue" bucket is now bounded to
      the last 14 days in `UnifiedToday.kt` (constant
      `OVERDUE_WINDOW_DAYS = 14`); the bucket header was also
      renamed to "Overdue, last 14 days (N)" so the cap is visible.
      Per-todolist views still surface anything older.
- [x] **W3.3** **R-8** — `formatReviewTimestamp()` humanizes the raw
      ISO timestamps (HH:mm today, EEE HH:mm within 6 days, MMM d
      HH:mm same year, yyyy-MM-dd older). Falls back to raw string
      on parse failure.
- [x] **W3.4** **U-1** — Intro Continue button is now
      `Modifier.fillMaxWidth()` so taps along the bottom strip hit
      the button instead of the manifesto body.
- [x] **W3.5** **U-2** — Superseded-mode FAB has a Material3
      `TooltipBox` + clearer content-description ("Show events
      hidden by base layer" / "Hide events hidden by base layer").
- [x] **W3.6** **U-3** — Rail labels: rotated `Text` is now bounded
      to 96dp wide with ellipsize, and the rail Box clips to
      `RectangleShape`; "3-day" / "Month" no longer bleed past the
      52dp rail into the band column.
- [x] **W3.7** **U-4** — Intro step-2 "Other scenarios" rows
      collapsed to plain de-emphasized bullets (no per-row emoji,
      no titleMedium) so they read as docs, not tiles.
- [x] **W3.8** **U-7** — Settings tile + section header renamed
      from "Accounts" to "Repositories" (testTag stays "Repos").
- [x] **W3.9** **U-11** — `PackManifest.parse` now accepts
      manifests without `[[sticker]]` rows, so the bundled scaffold
      packs for cat / fox / lion / tiger / wolf load and the wizard
      / pack picker enumerates every installed species. Per-sticker
      resolution still falls back to `R.drawable.about_bat`.
- [x] **W3.10** **U-12** — Demo's `calendars/base/calendar.toml`
      `name` field renamed from "Base layers" to
      "Sleep & work hours" so users don't see a meta concept in the
      Calendars settings list.
- [x] **W3.11** **M-1** — FAB cluster (Layout / Filter /
      Superseded) wrapped in `TooltipBox` + `PlainTooltip`.
- [x] **W3.12** **M-2** — When `adjustToLocalTimezone` is OFF,
      RepoCard now shows a coach card under the switch explaining
      the warped wall-clock and the device's timezone id.
- [x] **W3.13** **M-4** — Reviews pane gets a "Mark all read"
      TextButton when any item is unread; tap clears the unread
      chip locally (full on-disk read-state persistence is a
      follow-up — the existing schema doesn't track it).
- [x] **W3.14** **M-5** — EventDetail zone display: friendly format
      via `ZoneId.getDisplayName(SHORT, default-locale)` + city
      hint from the IANA id (e.g. "CEST · Longyearbyen" instead of
      "Arctic/Longyearbyen").

## Out of scope (won't fix this round, won't promise)

- Tablet master-detail completeness — own round.
- Performance / cold-start.
- Real-device wifi-adb runs.
- CalDAV / GitHub OAuth driving.
